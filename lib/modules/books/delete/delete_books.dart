import 'package:flutter/material.dart';
class DeleteBooks extends StatelessWidget {
  const DeleteBooks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('delete book');
  }
}
