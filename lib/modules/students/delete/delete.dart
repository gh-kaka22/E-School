import 'package:flutter/material.dart';
class DeleteStudent extends StatelessWidget {
  const DeleteStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Deleteeeeeeeee');
  }
}
