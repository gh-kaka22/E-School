import 'package:flutter/material.dart';
class DeleteClass extends StatelessWidget {
  const DeleteClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Delete class');
  }
}
