import 'package:flutter/material.dart';
class ShowAttendance extends StatelessWidget {
  const ShowAttendance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('attendace');
  }
}
