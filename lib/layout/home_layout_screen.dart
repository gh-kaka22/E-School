import 'package:flutter/material.dart';
import 'package:untitled/modules/students/delete/delete.dart';
import 'package:untitled/modules/students/register/register_screen.dart';

import '../modules/students/show/show_students.dart';
import '../shared/components/drawer.dart';

class HomeLayout extends StatelessWidget {
  const HomeLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    double w = MediaQuery.of(context).size.width;
    double h = MediaQuery.of(context).size.height;
    return Scaffold(


    );
  }
}

